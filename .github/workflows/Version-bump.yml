name: Auto Version Bump

on:
  push:
    branches:
      - main

jobs:
  bump-version:
    # Skip if the commit was made by the action itself to avoid infinite loops
    if: "!contains(github.event.head_commit.message, 'chore: bump version')"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Calculate and Update Version
        id: update_version
        run: |
          # 1. Extract current version from pyproject.toml
          CURRENT_VERSION=$(grep -m 1 '^version = ' pyproject.toml | sed -E 's/version = "(.*)"/\1/')
          echo "Current version: $CURRENT_VERSION"

          # 2. Split version into segments
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT_VERSION"

          # 3. Get the latest commit message
          COMMIT_MSG="${{ github.event.head_commit.message }}"

          # 4. Determine bump type
          if [[ "$COMMIT_MSG" == *"major"* ]]; then
            MAJOR=$((MAJOR + 1))
            MINOR=0
            PATCH=0
          elif [[ "$COMMIT_MSG" == *"feat"* ]]; then
            MINOR=$((MINOR + 1))
            PATCH=0
          elif [[ "$COMMIT_MSG" == *"fix"* ]]; then
            PATCH=$((PATCH + 1))
          else
            echo "No version bump keywords found. Skipping."
            exit 0
          fi

          NEW_VERSION="$MAJOR.$MINOR.$PATCH"
          echo "New version: $NEW_VERSION"

          # 5. Update pyproject.toml
          # Use sed to replace the version line specifically
          sed -i "0,/^version = .*/s/^version = .*/version = \"$NEW_VERSION\"/" pyproject.toml

          echo "new_ver=$NEW_VERSION" >> $GITHUB_OUTPUT

      - name: Commit and Push changes
        if: steps.update_version.outputs.new_ver != ''
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add pyproject.toml
          git commit -m "chore: bump version to ${{ steps.update_version.outputs.new_ver }} [skip ci]"
          git push

